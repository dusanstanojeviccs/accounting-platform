
import { increment } from 'frontend/helpers/increment';
import { module, test } from 'qunit';

module('Unit | Helper | increment');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = increment([42]);
  assert.ok(result);
});

